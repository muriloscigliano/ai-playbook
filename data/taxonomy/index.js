// Taxonomy barrel — autonomy (how much the AI may act) and visibility
// (how much the user sees it is AI). The two axes are orthogonal.

export { autonomyLevels } from './autonomy-levels.js'
export { visibilityLevels } from './visibility-levels.js'
